import { createTheme } from "@mantine/core";

export const theme = createTheme({
  fontFamily:
    '"Noto Sans JP", Inter, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif',
  primaryColor: "indigo",
  headings: {
    fontFamily:
      '"Noto Sans JP", Inter, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif',
  },
  radius: {
    md: "8px",
    lg: "10px",
  },
});
